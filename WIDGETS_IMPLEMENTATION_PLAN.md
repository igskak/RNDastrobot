# План имплементации данных/виджетов рабочего экрана

Источник: опрос астролога (`Steliara_данные_виджеты_опрос`). Включены все пункты,
отмеченные **Да** и **Может быть**, в порядке убывания приоритета.

Условные обозначения статуса до работ: 🟢 есть · 🟡 частично · 🔴 нет.
Каждый пункт: что делаем → swisseph/код → слой данных (service/route/migration) →
виджет на рабочем экране (`app/frontend/js/forecast-new-panel-*`).

Общие архитектурные заметки:
- Расчётный слой — расширяем `app/services/swisseph_engine.py` и профильные сервисы.
- API — новые поля в существующих ответах либо новые роуты под `app/api/routes/`.
- Кэш/персист — где расчёт дорогой (затмения, фикс. звёзды), кэшируем по аналогии с
  `013_add_transit_events_cache.sql`.
- Виджеты — каждый блок данных = отдельный включаемый/переставляемый виджет
  (как панели в `forecast-new-panel-layout.js`).

---

## ПРИОРИТЕТ 1 (Да)

### 1.1 Лунный фундамент: фаза Луны + Void of Course  *(prerequisite, см. примечание)*
Хотя фаза Луны отмечена P3, она и затмения делят один движок — строим первым.
- **Расчёт:** элонгация Moon−Sun → фаза (8 фаз), % освещённости через `swe.pheno_ut`.
  VOC: последний мажорный аспект Луны до выхода из знака — поверх уже существующего
  `aspect_service` + бисекция из `transit_service` (`_bisect_orb_boundary`).
- **Слой:** новый `lunar_service.py` (phase, illumination, next_phase_jd, voc_window).
- **Виджет:** «Луна сейчас» — знак/дом/фаза/%, индикатор VOC с временем входа/выхода.

### 1.2 Затмения + новолуния/полнолуния  🔴 → P1 «Да»
- **Расчёт:** `swe.sol_eclipse_when_glob` / `swe.lun_eclipse_when` для ближайших
  событий; нов/полнолуния — поиск точной элонгации 0°/180° (бисекция, движок из 1.1).
  Тип (total/partial/annular), градус/знак события, контакт с наталом (орб к точкам).
- **Слой:** `lunar_service.upcoming_lunations()` + кэш-таблица (миграция, по образцу 013).
- **Виджет:** «Ближайшие лунации/затмения» — список с датой, типом, градусом, домом
  натала, в который попадает.

### 1.3 Фиксированные звёзды  🔴 → P1 «Да»
- **Расчёт:** `swe.fixstar2_ud`/`swe.fixstar2` по каталогу `sefstars.txt` (проверить,
  что файл есть в `swisseph/`/ephe path; иначе добавить). Соединения звёзд с планетами
  и угловыми точками с орбом (отдельный, малый — конфигурируемый, дефолт ~1°).
- **Слой:** `fixed_stars_service.py` — список звёзд (имя, долгота, величина, природа
  по Робсону/Птолемею из справочной таблицы), матчинг к натальным точкам.
  Справочник природы звёзд — сид-таблица (миграция).
- **Виджет:** «Фиксированные звёзды» — какие звёзды на каких точках, орб, природа.

---

## ПРИОРИТЕТ 2 (Да)

### 2.1 Фазы планет относительно Солнца (cazimi / под лучами / oriental-occidental)  🔴 → P2
- **Расчёт:** угловое расстояние планеты от Солнца → cazimi (<~17′), combust (<~8.5°),
  under beams (<~15°); ориентальность/окцидентальность по знаку элонгации и порядку
  восхода. Пороги — конфигурируемые в preferences.
- **Слой:** дополнить `planet_characteristics_service.py` (там уже считаются stationary
  и пр. характеристики планеты) полем `solar_phase`.
- **Виджет:** колонка/бейдж в таблице планет + сводный виджет «Солнечные фазы».

### 2.2 Тропический / сидерический зодиак + аянамша  🟡 → P2
- **Замечание:** сейчас сидерики НЕТ нигде в коде — это самый крупный пункт P2.
- **Расчёт:** `swe.set_sid_mode(SIDM_*)` + флаг `FLG_SIDEREAL` во всех вызовах
  `calc_ut`/`houses` в `swisseph_engine.py`. Параметр zodiac/ayanamsha протянуть из
  настроек методологии.
- **Слой:** расширить preferences (миграция: zodiac_mode, ayanamsha) + `preferences_runtime`;
  затронет methodology_hash и инвалидацию артефактов (`029_*methodology_hash`).
  Важно: пересчёт всех зависимых артефактов (как `preference_recalc_service`).
- **Виджет:** индикатор активного зодиака/аянамши в шапке карты + переключатель.
- **Риск:** влияет на ВСЕ расчёты — нужен регресс-тест и аккуратная миграция дефолтов
  (всем существующим картам явно проставить tropical).

---

## ПРИОРИТЕТ 3 (Да)

### 3.1 Луна: знак / дом / фаза  🟡 → P3 «Да»
Закрывается виджетом 1.1 (фаза вынесена в P1 как фундамент). Здесь — лишь довести
дом Луны и оформление в общий «Луна сейчас». Фактически уже сделано в 1.1.

### 3.2 Void of Course  🔴 → P3 «Да»
Закрывается в 1.1 (общий лунный движок). Отдельной работы сверх 1.1 нет.

### 3.3 Planetary hours / управитель дня / лунные дни  🔴 → P3
- **Расчёт:** восход/закат через `swe.rise_trans` → деление дня/ночи на 12 планетных
  часов (халдейский ряд), управитель дня недели, лунный день (от последнего новолуния —
  движок из 1.1).
- **Слой:** `electional_service.py` (planetary_hours(date, geo), day_ruler, lunar_day).
- **Виджет:** «Планетные часы» — текущий час/управитель, таблица на сутки.

### 3.4 Композит / двойной композит  🔴 → P3
- **Расчёт:** композит = средние точки по долготе пар планет двух карт (midpoint composite);
  дома — composite Asc/MC. Двойной композит (Davison) — карта на среднюю дату/гео.
- **Слой:** `composite_service.py`, опираясь на `synastry_service` (пары уже связаны
  через person_chart_links) и `swisseph_engine` для Davison.
- **Виджет/роут:** новый тип карты в мульти-карточном сравнении.

---

## ПРИОРИТЕТ 3 (Может быть)

### 4.1 Профекции  🔴
- **Расчёт:** годовая профекция = (возраст mod 12) дом от Asc; управитель года =
  управитель знака профектического дома; месячные/дневные — арифметика. БД не нужна.
- **Слой:** `profections_service.py`.
- **Виджет:** «Профекции» — активный дом/знак/управитель года (+ месяц).

### 4.2 Антисы / контрантисы  🔴
- **Расчёт:** зеркало относительно оси Рак/Козерог (антис = 30°−долгота в знаке,
  отражённая); контрантис = +180°. Соединения антисов с натальными точками.
- **Слой:** дополнить `aspect_service`/`special_points_service`.
- **Виджет:** опция в сетке аспектов + список антисных контактов.

### 4.3 Астероиды: Церера / Веста / Паллада / Юнона  🔴
- **Расчёт:** `swe.calc_ut` с id `SE_CERES..SE_JUNO` (нужны файлы `seas_*`). Добавить
  в `PLANETS`-набор как опциональные точки.
- **Слой:** расширить `swisseph_engine.calculate_planets` + флаг включения в preferences.
- **Виджет:** доп. строки в таблице планет (тоггл).

### 4.4 Вертекс / East Point  🔴
- **Расчёт:** уже доступно из `swe.houses_ex` (ascmc[3]=Vertex, ascmc[4]=equatorial Asc).
  Дешёвый пункт — данные уже возвращаются, просто не извлекаются.
- **Слой:** добавить в `calculate_houses` выдачу Vertex/EastPoint.
- **Виджет:** доп. угловые точки в карте/таблице.

### 4.5 Доминанты (планеты / знаки / дома)  🟡
- **Расчёт:** свести существующие `planet_strength_service` + `special_roles_service`
  в единый рейтинг доминант (балльная сводка по планетам/знакам/стихиям/домам).
- **Слой:** агрегатор поверх имеющихся сервисов (нового расчёта почти нет).
- **Виджет:** «Доминанты карты» — топ планет/знаков/домов с очками.

---

## Привязка к фронту: viewKey / размещение / source

Реестр блоков — `VIEW_KEYS` в `app/frontend/js/forecast-new-panel-layout.js`.
Каждый новый блок = новый `viewKey` + лейбл в `VIEW_I18N` + рендерер +
поле(я) в payload. Размещение (панель/угол/компакт) задаётся добавлением ключа в
`CORNER_RECOMMENDED_VIEWS` / `CORNER_COMPACT_VIEWS`.

`source`: существующие блоки используют `'natal'`/`'prog'`. Часть новых блоков —
«момент сейчас», им вводим новый source `'now'` (transit-момент, не свойство натала).

| # | Блок | viewKey | source | Размещение | i18n-лейбл (RU) |
|---|------|---------|--------|-----------|-----------------|
| 1.1 | Луна сейчас (фаза+VOC) | `lunar` | `now` | угол (compact) | «Луна» |
| 1.2 | Лунации/затмения | `lunations` | `now` | панель | «Лунации» |
| 1.3 | Фиксированные звёзды | `fixstars` | `natal`/`prog` | панель | «Звёзды» |
| 2.1 | Солнечные фазы планет | колонка в `planets` + бейдж | `natal`/`prog` | в существующем блоке | — |
| 2.2 | Индикатор зодиака/аянамши | шапка карты (не блок) | — | header | — |
| 3.3 | Планетные часы | `hours` | `now` | угол (compact) | «Часы» |
| 3.4 | Композит/Davison | новый source в мульти-карте | `composite` | панель | «Композит» |
| 4.1 | Профекции | `profections` | `natal` | угол (recommended) | «Профекции» |
| 4.2 | Антисы/контрантисы | опция в `aspects`/`grid` | `natal`/`prog` | в существующем блоке | — |
| 4.3 | Астероиды | строки в `planets` (тоггл) | `natal`/`prog` | в существующем блоке | — |
| 4.4 | Вертекс/East Point | строки в `planets`/`houses` | `natal`/`prog` | в существующем блоке | — |
| 4.5 | Доминанты | `dominants` | `natal`/`prog` | угол (recommended) | «Доминанты» |

Примечание: пункты 2.1, 2.2, 4.2, 4.3, 4.4 — НЕ новые блоки, а расширение
существующих (`planets`/`houses`/`aspects`) или шапки. Новые `viewKey` нужны только
для: `lunar`, `lunations`, `fixstars`, `hours`, `profections`, `dominants`, `composite`.

## Рекомендуемые волны поставки

- **Волна A (быстрые победы, низкий риск):** 4.4 Вертекс/EastPoint, 4.5 Доминанты,
  2.1 Солнечные фазы, 4.2 Антисы — данные почти готовы, опираются на существующее.
- **Волна B (лунный кластер):** 1.1 Луна+VOC → 1.2 Затмения/лунации → 3.3 Планетные часы
  (общий восход/закат + лунный движок).
- **Волна C (контентные справочники):** 1.3 Фикс. звёзды, 4.3 Астероиды, 4.1 Профекции.
- **Волна D (крупное, отдельный эпик):** 2.2 Сидерический зодиак/аянамша,
  3.4 Композит/Davison.

Замечание по приоритетам: 2.2 (сидерика) формально P2, но это самый дорогой и рисковый
пункт (затрагивает все расчёты + миграцию методологии). Рекомендуется выносить в
отдельный эпик и не блокировать им волны A/B.
