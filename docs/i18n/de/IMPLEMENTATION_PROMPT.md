# Промпт для модели-исполнителя

Реализуйте немецкую локализацию Steliara в репозитории `/Users/ihorskakovskyi/RNDastro/swisseph` по готовому заданию `docs/i18n/de/PLAN.md`.

Сначала прочитайте AGENTS.md, PLAN.md и COPY_GUIDE.md. Пользователь уже выбрал **Sie**, повторно уточнять обращение не нужно. Код локали de, формат de-DE, default/fallback en. Переиспользуйте существующую реализацию. Не добавляйте i18n-библиотеки, не меняйте астрологические расчёты, цены, существующие юридические условия и пользовательские данные. В задачу входят все этапы DE-1…DE-6, включая публичные страницы, письма, AI и видимые строки вне каталогов. Деплой, merge и отправка реальных писем не поручены.

Подготовка сделана на main 52ac5240. Проверьте git status и различия с этой базой; не трогайте чужие изменения. Если работа идёт в другой ветке, адаптируйте только места, которые изменились. Полный повторный аудит не нужен.

В `docs/i18n/de/translation-source.json` лежат 2303 исходных ключа, исходный SHA256, русский контекст и 31 пакет максимум по 180 строк. Загружайте по одному пакету через python3. Это вход для перевода: конечный `app/frontend/locales/de.json` должен повторять вложенную структуру актуального en.json. Не ограничивайтесь переводом примеров. Все пакеты и новые ключи должны быть завершены. Используйте COPY_GUIDE как единый словарь; сохраните placeholders/HTML/URLs и значения машинных идентификаторов.

Пользователь отдельно потребовал естественный, живой немецкий. Готовые образцы уже отредактированы в COPY_GUIDE.md. Не возвращайтесь к буквальным калькам из английского или русского. Для каждого пакета выполните черновой перевод и отдельную редактуру всех немецких строк без постоянного подглядывания в исходник, затем сверьте смысл/параметры. Прочитайте связанные тексты вместе в контексте экрана, письма — целиком. Формальное Sie сохраняется, канцелярит и искусственная разговорность не нужны. В прогрессе разделяйте translated и editorially_reviewed; наличие перевода и прохождение технических тестов не доказывают естественность. Не заявляйте вычитку носителем, если её не было. Подробные примеры, контекстные различия Apply/Reset и критерии редакторской проверки находятся в COPY_GUIDE.md.

Работайте по этапам из PLAN.md. Сохраняйте краткий локальный прогресс с завершёнными этапами/пакетами и проверками, чтобы продолжение не повторяло работу. Не включайте de в выполненные результаты, пока остаётся английская копия или необработанные видимые системные тексты. Проверьте список хардкода из hardcoded-audit.json, но не исправляйте автоматически все 428 кандидатов: там есть демо, комментарии, бренды и внутренние ошибки. Вручную проверьте статические объекты и шаблоны, которых scanner не видит.

Особое внимание: отдельный массив SUPPORTED в head forecast-new.html; app-nav.js со своим языковым словарём и fallback uk; consultation.js с persist:false; mailer.py; детерминированные ответы/intent guards AstroAssistantService; per-locale allowlist для корректных совпадений EN/DE. Миграции 017/018 не переписывать, удалённые миграцией 027 AI caches не восстанавливать. DB-миграция нужна только при подтверждённом живом потребителе немецких справочных строк.

Расширьте существующие gates на de, добавьте meaningful regression tests по матрице PLAN.md. Выполните обязательную верификацию AGENTS.md и browser QA, пересоберите generated bundles штатной командой. Не вызывайте платные внешние модели/почту для unit-тестов. В итоговом ответе укажите реализованный объём, результаты проверок, отдельно любые непроверенные browser/live AI/DB сценарии и оставшиеся редакционные замечания. Не скрывайте неполный этап общим словом «готово».
